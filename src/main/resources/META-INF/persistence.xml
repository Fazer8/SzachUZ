<?xml version="1.0" encoding="UTF-8"?>
<persistence xmlns="https://jakarta.ee/xml/ns/persistence"
             xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
             xsi:schemaLocation="https://jakarta.ee/xml/ns/persistence
                                 https://jakarta.ee/xml/ns/persistence/persistence_3_0.xsd"
             version="3.0">

    <persistence-unit name="default" transaction-type="RESOURCE_LOCAL">
        <provider>org.hibernate.jpa.HibernatePersistenceProvider</provider>

        <!-- Dodaj swoje klasy encji -->
         <class>lol.szachuz.db.Entities.Users</class>
         <class>lol.szachuz.db.Entities.UserPreferences</class>
         <class>lol.szachuz.db.Entities.Friends</class>
         <class>lol.szachuz.db.Entities.Leaderboard</class>

        <properties>
            <!-- Połączenie do bazy w Dockerze -->
            <property name="jakarta.persistence.jdbc.driver" value="org.postgresql.Driver"/>
            <property name="jakarta.persistence.jdbc.url" value="jdbc:postgresql://postgres:5432/postgres"/>
            <property name="jakarta.persistence.jdbc.user" value="${env.PG_ADMIN_USER}"/>
            <property name="jakarta.persistence.jdbc.password" value="${env.PG_ADMIN_PASSWD}"/>

            <!-- Ustawienia Hibernate -->
            <property name="hibernate.dialect" value="org.hibernate.dialect.PostgreSQLDialect"/>
            <property name="hibernate.hbm2ddl.auto" value="update"/>
            <property name="hibernate.show_sql" value="true"/>
            <property name="hibernate.format_sql" value="true"/>
        </properties>
    </persistence-unit>
</persistence>
